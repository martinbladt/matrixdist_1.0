% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1.ph.R
\name{fit,ph-method}
\alias{fit,ph-method}
\title{Fit method for ph class}
\usage{
\S4method{fit}{ph}(
  x,
  y,
  weight = numeric(0),
  rcen = numeric(0),
  rcenweight = numeric(0),
  stepsEM = 1000,
  methods = c("RK", "RK"),
  rkstep = NA,
  uni_epsilon = NA,
  maxit = 100,
  reltol = 1e-08,
  every = 100,
  optim_method = "BFGS",
  r = 1,
  burn = 0,
  erlang = NULL,
  merlang_blocks = NULL,
  plot_progress = FALSE,
  plot_every = 100,
  plot_breaks = 50,
  plot_pause = 0.01
)
}
\arguments{
\item{x}{An object of class \linkS4class{ph}.}

\item{y}{Vector or data.}

\item{weight}{Vector of weights.}

\item{rcen}{Vector of right-censored observations. If a matrix is passed as argument, then observations are considered interval-censored}

\item{rcenweight}{Vector of weights for right-censored observations.}

\item{stepsEM}{Number of EM steps to be performed.}

\item{methods}{Methods to use for matrix exponential calculation: RM, UNI or PADE.}

\item{rkstep}{Runge-Kutta step size (optional).}

\item{uni_epsilon}{Epsilon parameter for uniformization method.}

\item{maxit}{Maximum number of iterations when optimizing g function.}

\item{reltol}{Relative tolerance when optimizing g function.}

\item{every}{Number of iterations between likelihood display updates.}

\item{optim_method}{Optimization method for optim function.}

\item{r}{Sub-sampling proportion for stochastic EM, defaults to 1.}

\item{burn}{Percentage of iterations (from 0 to 100) run on histogram
midpoints of \code{y} with bin counts as weights, before switching to the
standard fit on original data. Available only for uncensored data.}

\item{erlang}{Logical flag for exact Erlang EM updates with one repeated rate.
If \code{NULL}, this is auto-detected from the model name.}

\item{merlang_blocks}{Optional integer vector with Erlang block sizes for
exact mixture-of-Erlangs EM updates. If \code{NULL}, it is auto-read from object
attributes when available.}

\item{plot_progress}{Logical flag. If \code{TRUE}, a progress plot is redrawn
during EM: histogram plus fitted density for uncensored data, or Kaplan-Meier
plus fitted survival for right-censored data.}

\item{plot_every}{Positive integer. Plot refresh frequency in iterations when
\code{plot_progress = TRUE}. Defaults to 100.}

\item{plot_breaks}{Number of histogram breaks used in progress plotting.}

\item{plot_pause}{Small pause (in seconds) after each plot refresh to let
interactive devices repaint in real time.}
}
\value{
An object of class \linkS4class{ph}.
}
\description{
Fit method for ph class
}
\examples{
obj <- iph(ph(structure = "general", dimension = 2), gfun = "weibull", gfun_pars = 2)
data <- sim(obj, n = 100)
fit(obj, data, stepsEM = 100, every = 20)
}
